type: executable
name: .test

deps:
  - ulib

actions:
  - install:
     to: "${path}/../"

artifact-name: "test"

cxx-build-flags:
  linker:
    - "/DEBUG /PDB:\"${path}/../test.pdb\""

cxx-link-deps:
  - "user32.lib"

cxx-build-flags:
  compiler:
    - "/utf-8"