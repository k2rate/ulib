type: executable
name: .tests

cxxenv.msvc:
  cxx-build-flags:
    compiler:
      - "/utf-8"

load-context.!standalone:
  enabled: false

deps:
  - ulib

platform.windows:
  deps:
    - vcpkg:gtest @ static-md

platform.!windows:
  deps:
    - vcpkg:gtest

platform.linux|osx:
  cxx-global-link-deps:
    - pthread